
servobk motor(PORT1);        //Declara el modulo de temperatura en el puerto 1
int grados=0;                //Variable para guardar los grados

//Codigo principal que corre por siempre
code(){

  grados = motor.read();           //Lee la posicion actual del servo en grados
  bk7print(grados);                //Imprime en pantalla los grados
  delay(250);                      //Espera 250 milisegundos

}

