
displaybk segments(PORT1);     //Declares the Display module int PORT1

//Codigo principal que corre por siempre
code(){

 segments.printindividual(1,1);                   //Muestra en el segemento 1 un 1
 segments.printindividual(2,2);                   //Muestra en el segemento 2 un 2
 segments.printindividual(3,3);                   //Muestra en el segemento 3 un 3
 segments.printindividual(4,4);                   //Muestra en el segemento 4 un 4

}

